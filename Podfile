# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'Marvel Catalog' do
  use_frameworks!
  pod 'Alamofire', '~> 5.0.0-rc.3'
end
target 'Marvel CatalogTests' do
  inherit! :search_paths
  # Pods for testing
end
target 'Marvel CatalogUITests' do
  # Pods for testing
end
